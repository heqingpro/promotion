spring:
  application:
    name: MarketingCampaignService
  profiles:
    active: dev
  cloud:
    consul:
      host: localhost
      port: 8500
      config:
        prefix: xiamen
        fail-fast: true
        profile-separator: '-'
        format: yaml